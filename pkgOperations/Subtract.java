package pkgOperations;

public class Subtract implements Calculate
{
     public void cal(int x,int y)
    {
        int differ=x-y;
        System.out.println("difference of numbers="+differ);
    }
}

